# YourJannah — Next Session Problem Report

Copy everything below the --- line into the new session.

---

I'm continuing work on YourJannah — a mosque community platform with gamified dhikr (WordPress + yn-jannah plugin). Codebase at `C:\Users\user\Documents\yourjannah`, auto-deploys to `yourjannah.com` via push to main.

## Read these memory files first:
1. `C:\Users\user\.claude\projects\C--Users-user-Documents-yourniyyah\memory\yourjannah-hud-vision.md`
2. `C:\Users\user\.claude\projects\C--Users-user-Documents-yourniyyah\memory\yourjannah-auth-architecture.md`

## CRITICAL PROBLEM: Header/HUD is broken on mobile

The header area has been badly patched throughout today's session and needs a **complete rewrite**. Current issues visible on mobile (incognito, no cache):

### Issue 1: Ghost "Welcome to YourJannah" bar
A bar showing "🕌 Welcome to YourJannah | Sign In | Join Free" appears between the HUD and the main header. This text does NOT exist in any PHP, JS, or CSS file in the codebase — I've searched exhaustively. It may be:
- Generated by a WordPress core function or hook we're not seeing
- Coming from the theme's `functions.php` title/meta hooks
- A WordPress login bar remnant
- Related to the `ynj-topbar` CSS class in `theme-modern.css:63`

### Issue 2: Duplicate header bars on guest view
When logged out, users see multiple bars stacked:
1. Our guest HUD (dark navy: "🕌 YourJannah | Sign In | Join")
2. The mystery "Welcome to YourJannah" bar
3. The main header (logo + nav + mosque selector)

### Issue 3: Old removed elements still showing
Despite being deleted from the code, these still appear on some views:
- "Our Masjid / Rising Star League" brown card
- Section nav pills (Prayers, Feed, Dua Wall, League)
- These were removed from `page-mosque.php` but may exist in another template or JS

### Issue 4: "Salam, Phoenixcg · Free Member | Become a Patron →" bar
An orange/brown bar showing patron upsell. NOT found in any codebase file. Appears for logged-in users on mobile only.

## What the header SHOULD look like:

### Guest (not logged in):
```
[HUD: 🕌 YourJannah ----spacer---- Sign In | Join button]
[Header: Logo | Nav (Events, Classes, Live...) | Mosque Selector]
```
Two bars only. No "Welcome" bar. No "Join Free" bar.

### Logged in:
```
[HUD: Masjid Lv1 | #1 League | XP bar | Streak | Points | Members | ? | Avatar]
[Header: Logo | Nav | Notifications | Mosque Selector]
```
Two bars only. No admin bar (except for administrator role). No patron upsell bar.

## Key files to investigate:
- `theme/yourjannah-starter/header.php` — HUD + header rendering
- `theme/yourjannah-starter/assets/css/theme.css` — `.ynj-header`, `.ynj-hud` styles
- `theme/yourjannah-starter/assets/css/theme-modern.css` — `.ynj-topbar` class (line 63)
- `theme/yourjannah-starter/functions.php` — hooks that might inject bars
- `theme/yourjannah-starter/assets/js/homepage.js` — `showGreeting()` function (line 83) renders "Welcome to YourJannah" card
- `plugins/yn-jannah/yn-jannah.php` — admin bar hide logic, autologin handler

## Approach for the new session:
1. **Read the ENTIRE header.php** — understand every element rendered
2. **Search for ALL hooks** that add content before/after the header (wp_head, wp_body_open, wp_footer, send_headers)
3. **Test as guest on mobile viewport** in Chrome before pushing ANY changes
4. **Rewrite the header if needed** — make it clean with exactly 2 bars (HUD + Header), no ghost elements
5. **Test on mobile viewport BEFORE every commit**

## What was accomplished today (working correctly):
- PIN auth system rewritten (PIN = WP password, one system)
- Autologin redirect (/?ynj_autologin) working
- Duplicate ynj_users prevention
- Member counts are real (no fake seeding)
- Check-in points: 500 normal, 2000 Jumu'ah
- Unlimited dhikr (1-min cooldown)
- Patron flow fixed (Stripe webhook metadata)
- League table redesigned (emerald green)
- Admin bar hidden for non-administrators
- Sitemap at /sitemap-mosques.xml
- Sponsor tiers (Gold/Silver/Bronze)
- PIN enforced to exactly 4 digits
- Cache-control headers on all pages
- Service worker force-unregister

## Local dev:
- Docker: `cd C:\Users\user\Documents\yourjannah && docker compose up -d`
- WordPress: http://localhost:8090 (admin/admin)
- Push to main auto-deploys to yourjannah.com
